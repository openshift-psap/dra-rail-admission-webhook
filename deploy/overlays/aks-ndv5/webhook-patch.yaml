apiVersion: apps/v1
kind: Deployment
metadata:
  name: dra-gpu-nic-webhook
  namespace: dra-webhook-system
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: webhook
        image: ghcr.io/openshift-psap/dra-rail-admission-webhook/webhook:dev-expand-cel-selector-attributes
