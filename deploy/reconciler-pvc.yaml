apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dra-reconciler-state
  namespace: dra-webhook-system
  labels:
    app: dra-gpu-nic-reconciler
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
