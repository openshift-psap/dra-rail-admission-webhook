# DEPRECATED: Kustomize deployment is deprecated and will be removed in the
# next release. Use the Helm chart instead:
#
#   helm install dra charts/dra-admission-webhook/ -n dra-webhook-system --create-namespace
#
# For cluster-specific config, use values files:
#   helm install dra charts/dra-admission-webhook/ -f charts/dra-admission-webhook/values-aks-ndv5.yaml
#
# See charts/dra-admission-webhook/values.yaml for all configurable fields.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dra-webhook-system

resources:
- configmap.yaml
- rbac.yaml
- webhook-deployment.yaml
- webhook-service.yaml
- webhook-config.yaml
- reconciler-pvc.yaml
- reconciler-deployment.yaml
- webhook-pdb.yaml
