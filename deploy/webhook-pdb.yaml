apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: dra-gpu-nic-webhook
  namespace: dra-webhook-system
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: dra-gpu-nic-webhook
